# Milestone v1.0: Local Wargame Analysis

**Status:** ✅ SHIPPED 2026-01-25
**Phases:** 1-7
**Total Plans:** 10

## Overview

Transform the Kubernetes-deployed Blackbox AI wargaming platform into a fully local stack running on an HP EliteBook (16GB RAM) without external API dependencies. The major work was replacing AssemblyAI with WhisperX + pyannote for speaker diarization, and configuring chat to use a remote Ollama server.

## Phases

### Phase 1: Infrastructure

**Goal**: Get all infrastructure services running via Docker Compose — MongoDB, RabbitMQ, MinIO, Netflix Conductor, Apache Tika
**Depends on**: Nothing (first phase)
**Plans**: 2 plans

Plans:
- [x] 01-01: Set up Docker Compose with all infrastructure services
- [x] 01-02: Verify services are accessible and healthy

**Details:**
- 7 services running: MongoDB, RabbitMQ, MinIO, Elasticsearch, Redis, Conductor, Tika
- Memory usage: ~2.4GB (well under 6GB target)
- Conductor UI on port 5001 (macOS AirPlay conflict on port 5000)

### Phase 2: Backend Setup

**Goal**: Configure and run Spring Boot backend with local environment variables pointing to Docker services
**Depends on**: Phase 1
**Plans**: 1 plan

Plans:
- [x] 02-01: Create .env, build and run backend, verify connectivity

**Details:**
- Backend on port 8081 (avoid Conductor API conflict on 8080)
- Java 21 via Homebrew openjdk@21
- MongoDB connected, Conductor workers initialized

### Phase 3: Diarization Rewrite

**Goal**: Replace AssemblyAI API with WhisperX + pyannote for fully local speaker diarization (CPU-based, batch processing)
**Depends on**: Phase 1
**Plans**: 3 plans

Plans:
- [x] 03-01: Implement WhisperXDiarizationService class
- [x] 03-02: Service setup and verification with PyTorch 2.8 compat
- [x] 03-03: Test diarization with sample audio files

**Details:**
- WhisperX + pyannote for speaker diarization
- CPU-only processing at 0.68x realtime (20s for 30s audio)
- PyTorch weights_only patch via monkey-patch for PyTorch 2.6+ compatibility
- Service on port 8082

### Phase 4: Chat Service

**Goal**: Configure chat service to use remote Ollama server instead of local/Azure OpenAI
**Depends on**: Phase 1
**Plans**: 1 plan

Plans:
- [x] 04-01: Configure OLLAMA_BASE_URL for remote server and verify connectivity

**Details:**
- Remote Ollama: ollama.island.a-p.team:11434 with mistral:7b model
- Python 3.12 required for 3.10+ union type syntax
- Service on port 8083

### Phase 5: Frontend Setup

**Goal**: Configure Vue/Quasar frontend to point to localhost backend and run dev server
**Depends on**: Phase 2
**Plans**: 1 plan

Plans:
- [x] 05-01: Update API base URL and start dev server

**Details:**
- Frontend on port 9003 (Docker uses default port 9000)

### Phase 6: Integration Testing

**Goal**: Verify complete end-to-end workflow: upload audio → diarization → analysis → chat
**Depends on**: Phases 2, 3, 4, 5
**Plans**: 1 plan

Plans:
- [x] 06-01: Test complete workflow with sample wargame audio

**Details:**
- Conductor workflow/tasks registered via API (not auto-registered by backend)
- Full workflow execution verified: upload → diarization → transcript display

### Phase 7: Diarization Service

**Goal**: Fix incomplete data pipeline — speaker names show as "Speaker 00" instead of extracted names, and summarization/insights don't appear in frontend
**Depends on**: Phase 6
**Plans**: 1 plan

Plans:
- [x] 07-01: Diagnose and fix data pipeline (speaker names + summarization)

**Details:**
- Fixed Conductor INLINE JavaScript task JSON serialization using Jackson ObjectMapper
- Chat service URL in .env must use port 8083
- Full pipeline working: transcription → speakers → rounds → summaries

---

## Milestone Summary

**Key Decisions:**
- Remote Ollama for chat (offloads RAM, allows larger models)
- WhisperX + pyannote for diarization (no API dependency, CPU-only acceptable)
- Docker for infrastructure only (simpler debugging with native app services)
- Batch diarization processing (CPU-only is slow, overnight jobs acceptable)
- Conductor UI on port 5001 (macOS AirPlay conflict on port 5000)
- Backend on port 8081 (avoid Conductor API conflict on 8080)
- Diarization service on port 8082
- Chat service on port 8083
- Frontend on port 9003
- Jackson ObjectMapper for Conductor INLINE tasks (JSON.stringify fails on Java Maps)

**Issues Resolved:**
- macOS port 5000 conflict with AirPlay Receiver
- PyTorch 2.6+ compatibility with pyannote via monkey-patch
- Conductor workflow registration (not auto-registered)
- INLINE JavaScript task JSON serialization for Java Map objects

**Issues Deferred:**
- None

**Technical Debt Incurred:**
- None significant

---

_For current project status, see .planning/ROADMAP.md_
