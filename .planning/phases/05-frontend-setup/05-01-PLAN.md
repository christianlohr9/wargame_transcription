---
phase: 05-frontend-setup
plan: 01
type: execute
---

<objective>
Configure Vue/Quasar frontend to point to localhost backend and run dev server.

Purpose: Enable local frontend development with hot reload, connected to local backend services.
Output: Running frontend dev server accessible at http://localhost:9000 communicating with backend at http://localhost:8081.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase context:
@.planning/phases/02-backend-setup/02-01-SUMMARY.md

**Tech stack available:**
- Node.js v24.3.0
- npm 11.4.2
- Vue 3 v3.5.22
- Quasar v2.16.0
- node_modules already installed

**Established patterns:**
- Backend runs on port 8081 (avoids Conductor 8080 conflict)
- Conductor UI on port 5001 (macOS AirPlay conflict)

**Constraining decisions:**
- [Phase 02-01]: Backend on port 8081

**Key files:**
- blackbox/frontend/src/boot/axios.ts (API base URL configuration)
- blackbox/frontend/quasar.config.ts (Quasar/Vite config)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update API base URL to localhost</name>
  <files>blackbox/frontend/src/boot/axios.ts</files>
  <action>Change BASE_URL from 'http://blackbox.dev.bwi.com/' to 'http://localhost:8081/'. The commented localhost line already exists but points to port 8080 - update to 8081 to match backend port from Phase 2.</action>
  <verify>Read axios.ts and confirm BASE_URL is 'http://localhost:8081/'</verify>
  <done>BASE_URL constant set to 'http://localhost:8081/'</done>
</task>

<task type="auto">
  <name>Task 2: Start frontend dev server and verify backend connectivity</name>
  <files>blackbox/frontend/</files>
  <action>Run `npm run dev` from blackbox/frontend/ directory. Quasar dev server starts on default port 9000. Wait for compilation to complete. Verify server is accessible.</action>
  <verify>curl http://localhost:9000 returns HTML content</verify>
  <done>Dev server running on port 9000, frontend loads in browser</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] BASE_URL points to http://localhost:8081/
- [ ] Dev server running without build errors
- [ ] Frontend accessible at http://localhost:9000
- [ ] No TypeScript/ESLint errors during build
</verification>

<success_criteria>

- API base URL configured for local backend
- Frontend dev server running with hot reload
- No build errors
- Phase 5 complete, ready for integration testing
  </success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-setup/05-01-SUMMARY.md`:

# Phase 5 Plan 01: Frontend Setup Summary

**[Substantive one-liner]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file.ts` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Phase Readiness

[Ready for Phase 6: Integration Testing]
</output>
