---
phase: 12-integration-testing
plan: 02
type: execute
---

<objective>
Validate transcription+diarization and full pipeline modes end-to-end, then document all findings for Docker-readiness in Phase 13.

Purpose: Confirm service addition correctly upgrades pipeline mode, speaker labels appear with diarization, and full pipeline produces summaries. Document everything Phase 13 needs.
Output: All three pipeline modes validated, Docker-readiness notes for Phase 13.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-integration-testing/12-CONTEXT.md
@.planning/phases/12-integration-testing/12-01-SUMMARY.md

# Prior phase summaries:
@.planning/phases/10-modular-pipeline/10-02-SUMMARY.md
@.planning/phases/11-frontend-adaptation/11-02-SUMMARY.md

# Key service files:
@blackbox/backend/blackbox_service/src/main/java/com/cgi/blackbox/service/implementation/PipelineModeResolver.java
@blackbox/backend/blackbox_application/src/main/java/com/cgi/blackbox/rest/PipelineStatusAPI.java
@blackbox/frontend/src/components/AppBar.vue
@blackbox/frontend/src/pages/IndexPage.vue
@blackbox/frontend/src/components/TranscriptBox.vue
@speaker-diarization-service/src/services/whisperx_diarization_service.py
@speaker-diarization-service/src/tasks/diarization_task.py

# Configuration:
@blackbox/.env
@speaker-diarization-service/.env
@ask-chat-service/.env
@blackbox/backend/blackbox_application/src/main/resources/application.yml

**Constraining decisions:**
- [Phase 10]: Conductor SWITCH branches by pipeline_mode (full, transcription_diarization, transcription_only)
- [Phase 10]: transcription_only skips pyannote entirely
- [Phase 11]: pyannote lazy-loaded on first diarization request
- [Phase 11]: vad_filter=True prevents early transcription cutoff
- [Phase 11]: Conductor taskReferenceNames globally unique across branches

**Prerequisites:**
- Docker infrastructure running (from Plan 01)
- Backend running on port 8081 (from Plan 01)
- Frontend running on port 9003 (from Plan 01)
- Transcription-only mode already validated (Plan 01)
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Transcription-only validated in Plan 01. Now testing transcription+diarization mode by adding the diarization service.</what-built>
  <how-to-verify>
**Start diarization service (keep backend and frontend running from Plan 01):**

1. Start speaker-diarization-service:
   ```bash
   cd speaker-diarization-service && python3 src/main.py
   ```
   Wait for FastAPI startup log. Note: pyannote model loads lazily on first request, so startup should be fast even if HF_TOKEN is valid.

2. Verify service is reachable:
   ```bash
   curl -s http://localhost:8082/health
   ```
   Expected: 200 OK

**Validate mode upgrade:**

3. Check pipeline status:
   ```bash
   curl -s http://localhost:8081/pipeline/status | python3 -m json.tool
   ```
   Expected: `resolvedMode: "transcription_diarization"`, diarization: true, askChat: false

4. In browser (http://localhost:9003), click the refresh button next to service chips:
   - Diarization chip should turn **green** with filled circle
   - Chat chip should remain **grey**
   - Mode chip should show **"Mode: Transcription Diarization"**

**Run transcription+diarization workflow:**

5. Upload a test audio file (ideally one with multiple speakers)
6. Click "Run" / start analysis
7. Monitor Conductor UI at http://localhost:5001 — workflow should execute the transcription_diarization SWITCH branch
8. Verify:
   - Transcription completes with text output
   - **Speaker labels appear** in transcript (e.g., "Speaker 1:", "Speaker 2:")
   - Frontend shows **split layout** (transcript + speaker panel, no summary panel)
   - TranscriptBox shows speaker-attributed text
   - No errors in backend or diarization service logs
   - No summary/analysis section visible (chat service not running)

**Check graceful degradation messaging:**

9. Verify user can understand:
   - Diarization is online (green chip)
   - Chat is offline (grey chip with tooltip)
   - Mode reflects available services

Report what you see. Type "approved" if everything works, or describe any issues.
  </how-to-verify>
  <resume-signal>Type "approved" to continue to full pipeline test, or describe issues to fix</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Transcription+diarization validated. Now testing full pipeline mode by adding the chat service.</what-built>
  <how-to-verify>
**Start chat service (keep all other services running):**

1. Start ask-chat-service:
   ```bash
   cd ask-chat-service && python3 src/main.py
   ```
   Wait for FastAPI startup log.

2. Verify service is reachable:
   ```bash
   curl -s http://localhost:8083/health
   ```
   Expected: 200 OK

3. Verify remote Ollama is accessible (this is required for full pipeline):
   ```bash
   curl -s http://ollama.island.a-p.team:11434/api/tags | head -c 200
   ```
   If Ollama is not reachable, the chat service may start but workflow tasks will fail. Note the result.

**Validate mode upgrade to full:**

4. Check pipeline status:
   ```bash
   curl -s http://localhost:8081/pipeline/status | python3 -m json.tool
   ```
   Expected: `resolvedMode: "full"`, both services true

5. In browser, refresh service status:
   - Both Diarization and Chat chips should be **green**
   - Mode chip should show **"Mode: Full"**

**Run full pipeline workflow:**

6. Create a workspace with a wargame setup that has definitionOfRounds configured
7. Upload a test audio file with multiple speakers
8. Click "Run" / start analysis
9. Monitor Conductor UI — workflow should execute the full SWITCH branch (all 7 tasks)
10. Verify:
    - Transcription completes
    - Speaker labels appear with diarization
    - **Speaker identification** resolves (if wargame setup has participant names)
    - **Summary/analysis tab appears** with LLM-generated content
    - **Round detection** identifies rounds (if applicable to audio)
    - Frontend shows **full layout** (transcript + speakers + summary panels)
    - All panels populated, no "missing data" placeholders

**If Ollama is unreachable:**

11. If step 3 failed, verify graceful degradation:
    - Mode should downgrade to transcription_diarization despite chat service being "up"
    - OR: chat service health check passes but workflow task fails — note which behavior occurs
    - This is important to document for Phase 13

Report what you see. Type "approved" if everything works, or describe any issues.
  </how-to-verify>
  <resume-signal>Type "approved" to continue to Docker-readiness documentation, or describe issues to fix</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Docker-readiness documentation</name>
  <files>.planning/phases/12-integration-testing/12-DOCKER-READINESS.md</files>
  <action>
Create a Docker-readiness document summarizing everything Phase 13 needs to know. Based on all findings from Plans 01 and 02, document:

**1. Service inventory:**
- List all services that need containerizing (backend, diarization, chat) with their:
  - Language/runtime and version requirements (JDK 21, Python <3.13)
  - Port mappings
  - Dependencies (which Docker infra services they need)
  - Startup order constraints
  - Health check endpoints

**2. Environment configuration:**
- All required environment variables per service
- Which variables change between local and Docker (e.g., localhost → container names)
- Any env vars with secrets that need special handling (HF_TOKEN)

**3. Known quirks:**
- Line ending issues and how to prevent in Docker
- JDK version sensitivity
- Python version constraints (conductor-python <3.13)
- pyannote HuggingFace model gating
- Lazy model loading behavior (first request is slow)
- Remote Ollama dependency (external to Docker network)

**4. Issues found during testing:**
- Any bugs discovered and fixed during Plans 01-02
- Any issues still open
- Workarounds in use

**5. Startup sequence:**
- Required order: Docker infra → backend → (optional) diarization → (optional) chat
- Health check timing considerations
- Model loading delays

**6. Pipeline mode behavior:**
- Document observed auto-detection behavior across all three modes
- Degradation behavior when services stop
- Frontend refresh behavior

Write to .planning/phases/12-integration-testing/12-DOCKER-READINESS.md
  </action>
  <verify>File exists and contains all 6 sections with specific findings from testing</verify>
  <done>Docker-readiness document created with concrete findings from integration testing, ready to inform Phase 13 planning</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Transcription+diarization mode works end-to-end with speaker labels
- [ ] Full pipeline mode works end-to-end with summaries (or graceful degradation documented if Ollama unreachable)
- [ ] Pipeline mode auto-detection correctly upgrades as services are added
- [ ] Frontend service chips accurately reflect service availability
- [ ] User messaging is clear about why each mode is active
- [ ] All three modes produce correct frontend layouts
- [ ] Docker-readiness document captures all findings for Phase 13
- [ ] No blocking issues remain
</verification>

<success_criteria>
- All three pipeline modes validated end-to-end (or degradation documented)
- User confirms frontend rendering and messaging are correct for each mode
- Docker-readiness document created with actionable findings
- Phase 12 complete, ready for Phase 13 (one-click services)
</success_criteria>

<output>
After completion, create `.planning/phases/12-integration-testing/12-02-SUMMARY.md`

Update `.planning/STATE.md`:
- Current Position: Phase 12 complete, ready for Phase 13
- Progress: 100% (v2.0 Phase 12 complete)
- Add any new decisions to Accumulated Context
</output>
