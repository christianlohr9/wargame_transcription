---
phase: 14-guppy-llm-integration
plan: 04
type: execute
---

<objective>
Create model bundling script and update Python build script to include llama-cpp-python for air-gapped deployment.

Purpose: Ensure the GGUF model file and llama-cpp-python native extension are included in the portable runtime for zero-install air-gapped deployment.
Output: bundle-llm-model.sh script, updated build-python-runtime.sh, verified conda-pack compatibility.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-guppy-llm-integration/14-RESEARCH.md
@.planning/phases/13-one-click-services/13-06-SUMMARY.md

@blackbox-desktop/scripts/build-python-runtime.sh
@blackbox-desktop/scripts/build-python-runtime.md
@blackbox-desktop/scripts/bundle-models.sh
@blackbox-desktop/electron/processManager.js

**Tech stack available:** conda-pack, huggingface-hub, llama-cpp-python, jlink
**Established patterns:** build-python-runtime.sh creates conda env + conda-pack, bundle-models.sh downloads HF models, processManager resolves bundled model paths
**Constraining decisions:**
- [Phase 13-06]: CPU-only PyTorch in conda-pack (~2-3GB)
- [Phase 13-06]: huggingface_hub.snapshot_download with local_dir_use_symlinks=False
- [Research]: SmolLM3-3B Q4_K_M GGUF ~2GB, from ggml-org/SmolLM3-3B-GGUF on HuggingFace
- [Research]: conda-pack + llama-cpp-python native extension needs testing (open question)
- [Research]: Fallback: bundle llama-server binary or use llamafile if conda-pack fails
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create bundle-llm-model.sh for SmolLM3-3B GGUF download</name>
  <files>blackbox-desktop/scripts/bundle-llm-model.sh, blackbox-desktop/scripts/bundle-llm-model.md</files>
  <action>
1. Create `bundle-llm-model.sh` (executable) following the pattern of existing `bundle-models.sh`:
   - Download SmolLM3-3B Q4_K_M GGUF from HuggingFace:
     - Repo: `ggml-org/SmolLM3-3B-GGUF`
     - File: the Q4_K_M variant (single .gguf file)
     - Use `huggingface-hub` CLI or Python to download specific file: `huggingface-cli download ggml-org/SmolLM3-3B-GGUF SmolLM3-3B-Q4_K_M.gguf --local-dir ./resources/models/`
   - Copy to `blackbox-desktop/resources/models/smollm3-3b-q4_k_m.gguf`
   - Verify file exists and print size
   - Script should be idempotent (skip download if file already exists and size matches)

2. Create `bundle-llm-model.md` documentation:
   - Prerequisites: huggingface-hub installed, internet access (one-time download)
   - Usage: `./scripts/bundle-llm-model.sh`
   - Output: GGUF model file in resources/models/
   - Size estimate: ~2GB for Q4_K_M 3B model
   - Note: This script runs once on a machine with internet access. The resulting model file is bundled into the installer for air-gapped deployment.

AVOID: Downloading at runtime — model must be bundled in installer. AVOID: Full-precision models (Q4_K_M is the target quantization).
  </action>
  <verify>Check bundle-llm-model.sh is executable, uses huggingface-cli or huggingface_hub Python, targets correct repo/file, outputs to resources/models/. Check .md documentation exists.</verify>
  <done>bundle-llm-model.sh script downloads SmolLM3-3B Q4_K_M GGUF to resources/models/, documentation created, script is idempotent</done>
</task>

<task type="auto">
  <name>Task 2: Update build-python-runtime.sh to include llama-cpp-python</name>
  <files>blackbox-desktop/scripts/build-python-runtime.sh, blackbox-desktop/scripts/build-python-runtime.md</files>
  <action>
1. In `build-python-runtime.sh`:
   - Add `llama-cpp-python` to the pip install step alongside existing ask-chat-service dependencies
   - Ensure CPU-only build flags are set (no CUDA): `CMAKE_ARGS="-DGGML_CUDA=OFF" pip install llama-cpp-python`
   - The conda-pack step should capture the installed llama-cpp-python native extension automatically
   - Add a verification step after conda-pack: test that `python -c "from llama_cpp import Llama; print('llama_cpp OK')"` works from the packed environment

2. In `build-python-runtime.md`:
   - Add note about llama-cpp-python requiring C++ compiler (cmake, gcc/MSVC) during build
   - Document the CMAKE_ARGS flag for CPU-only build
   - Note the conda-pack compatibility consideration: if the native extension doesn't relocate, fallback is to bundle llama-server binary separately

AVOID: Including CUDA support in the build (adds ~2GB and isn't needed for CPU target). AVOID: Assuming conda-pack will work without testing — include the verification step.
  </action>
  <verify>Read build-python-runtime.sh for llama-cpp-python install with CPU-only flags. Check verification step exists. Read .md for updated documentation.</verify>
  <done>build-python-runtime.sh installs llama-cpp-python with CPU-only build, includes verification step, documentation updated with build requirements and fallback notes</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bundle-llm-model.sh` exists and is executable
- [ ] Script targets correct HuggingFace repo and Q4_K_M quantization
- [ ] `build-python-runtime.sh` includes llama-cpp-python with CPU-only flags
- [ ] Post-pack verification step tests llama_cpp import
- [ ] Documentation updated for both scripts
- [ ] No changes to existing model bundling (whisper, pyannote) behavior
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Build scripts ready for Windows execution (actual builds run on target machine)
- Model download script is idempotent
</success_criteria>

<output>
After completion, create `.planning/phases/14-guppy-llm-integration/14-04-SUMMARY.md`
</output>
