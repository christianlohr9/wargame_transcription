---
phase: 14-guppy-llm-integration
plan: 01
type: execute
---

<objective>
Add llama-cpp-python as a third LLM backend in ask-chat-service for CPU-only local inference.

Purpose: Enable offline summarization on air-gapped CPU-only machines without requiring an external Ollama server.
Output: Working LlamaCppChatService implementation with backend selection and health reporting.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-guppy-llm-integration/14-RESEARCH.md
@.planning/phases/14-guppy-llm-integration/14-CONTEXT.md

@ask-chat-service/src/services/chat_service.py
@ask-chat-service/src/services/__init__.py
@ask-chat-service/src/services/ollama_chat_service.py
@ask-chat-service/requirements.txt
@ask-chat-service/src/main.py
@ask-chat-service/src/api/ask_chat_api.py

**Tech stack available:** FastAPI, ollama, openai, llama-cpp-python (new)
**Established patterns:** ChatService abstract base class, backend selection via CHAT_SERVICE env var, OllamaChatService as reference implementation
**Constraining decisions:**
- [Phase 13-02]: Python services are REST-only FastAPI (no Conductor)
- [Phase 13-06]: conda-pack for portable Python runtime
- [Research]: Use llama-cpp-python with SmolLM3-3B Q4_K_M GGUF model (~2GB)
- [Research]: Load model once at startup, reuse for all requests (5-30s load time)
- [Research]: Set n_ctx=4096 (not 128k) to conserve RAM on 16GB machine
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add llama-cpp-python dependency and create LlamaCppChatService</name>
  <files>ask-chat-service/requirements.txt, ask-chat-service/src/services/llamacpp_chat_service.py</files>
  <action>
1. Add `llama-cpp-python` to requirements.txt (no version pin — let pip resolve compatible version).

2. Create `llamacpp_chat_service.py` following the OllamaChatService pattern:
   - Import `Llama` from `llama_cpp`
   - Extend `ChatService` abstract base class
   - In `__init__`: Load model from `LLAMACPP_MODEL_PATH` env var (default: `./models/smollm3-3b-q4_k_m.gguf`). Configure `n_ctx` from `LLAMACPP_CTX_SIZE` env (default 4096), `n_threads` from `LLAMACPP_THREADS` env (default 4), `n_batch=512`, `verbose=False`.
   - Expose a `model_loaded` boolean attribute (True after __init__ succeeds).
   - Implement `ask_chat(self, chat_history: ChatHistoryModel) -> Union[str, dict]`:
     - Convert chat_history.messages to list of dicts with role/content
     - Call `self.llm.create_chat_completion(messages=messages, temperature=0.7, max_tokens=2048)`
     - If chat_history.response_schema is not None, pass `response_format={"type": "json_object", "schema": chat_history.response_schema}` and parse JSON response (matching OllamaChatService's JSON handling pattern)
     - Return content string or parsed dict

AVOID: Loading model per-request (load once in __init__). AVOID: Setting n_ctx to 128k (wastes RAM). AVOID: Streaming — backend collects full responses.
  </action>
  <verify>python -c "from services.llamacpp_chat_service import LlamaCppChatService; print('Import OK')" (run from ask-chat-service/src/)</verify>
  <done>LlamaCppChatService class exists, imports cleanly, follows ChatService interface, llama-cpp-python in requirements.txt</done>
</task>

<task type="auto">
  <name>Task 2: Update backend selection factory and health endpoint</name>
  <files>ask-chat-service/src/services/__init__.py, ask-chat-service/src/api/ask_chat_api.py</files>
  <action>
1. In `__init__.py`, add `llamacpp` case to `_get_chat_service()`:
   - Import LlamaCppChatService
   - When `CHAT_SERVICE == "llamacpp"`, return `LlamaCppChatService()`
   - Use lazy import (inside the if block) to avoid loading llama_cpp when not needed

2. In `ask_chat_api.py` (or wherever the `/health` endpoint is defined), enhance the health response:
   - If the chat_service is a LlamaCppChatService instance, include `"model_loaded": True/False` in health response
   - This lets the Electron health checker know when the model is ready vs still loading
   - For other backends, health response remains unchanged

This ensures the service only reports healthy when the model is actually loaded and ready to serve requests.
  </action>
  <verify>CHAT_SERVICE=llamacpp python -c "from services import chat_service; print(type(chat_service).__name__)" should print LlamaCppChatService (will fail without model file, but import chain should work). Check that health endpoint code includes model_loaded field.</verify>
  <done>Backend factory supports "llamacpp" option. Health endpoint reports model_loaded status for llamacpp backend. Other backends unaffected.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `llama-cpp-python` listed in requirements.txt
- [ ] `LlamaCppChatService` class exists and extends `ChatService`
- [ ] `__init__.py` factory handles `CHAT_SERVICE=llamacpp`
- [ ] Health endpoint includes model_loaded status for llamacpp backend
- [ ] No changes to OllamaChatService or OpenAIChatService behavior
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- LlamaCppChatService follows same interface as OllamaChatService
- Backend selection works for all three options: ollama, openai, llamacpp
</success_criteria>

<output>
After completion, create `.planning/phases/14-guppy-llm-integration/14-01-SUMMARY.md`
</output>
