---
phase: 14-guppy-llm-integration
plan: 05
type: execute
---

<objective>
End-to-end verification of llamacpp backend integration across all layers.

Purpose: Validate that the full stack works together — chat service with llamacpp backend, Electron settings, frontend UI, and model loading.
Output: Verified integration, any issues documented, phase complete.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-guppy-llm-integration/14-CONTEXT.md
@.planning/phases/14-guppy-llm-integration/14-RESEARCH.md

@ask-chat-service/src/services/llamacpp_chat_service.py
@ask-chat-service/src/services/__init__.py
@ask-chat-service/src/api/ask_chat_api.py
@blackbox-desktop/electron/processManager.js
@blackbox-desktop/electron/main.js

**Constraining decisions:**
- [Research]: Model loading takes 5-30s — health check must reflect model readiness
- [Research]: RAM budget ~10-12GB with all services running on 16GB machine
- [Context]: Full pipeline (transcribe -> diarize -> summarize) must work end-to-end offline
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration smoke test — chat service with llamacpp backend</name>
  <files>N/A (testing only)</files>
  <action>
1. Start ask-chat-service with llamacpp backend (requires a GGUF model file):
   - If model file is available locally, test with it
   - If not, create a minimal test that validates:
     a. Service starts with CHAT_SERVICE=llamacpp
     b. Health endpoint reports correctly (model_loaded status)
     c. `/chat` endpoint accepts proper request format

2. Verify the full integration chain:
   - Check ask-chat-service __init__.py properly routes to LlamaCppChatService
   - Check processManager.js injects correct env vars for llamacpp backend
   - Check main.js electron-store defaults are correct
   - Check preload.js exposes settings API
   - Check SettingsPanel.vue calls correct IPC methods
   - Check serverSentEvents.ts triggers notification on prompt completion

3. Document any issues found in the summary. If model file is unavailable for live testing, note that live validation is deferred to target machine deployment.

This is a code review + dry-run verification task. Full live testing with the model happens on the target HP EliteBook.
  </action>
  <verify>All integration points verified: service factory → service class → health endpoint → ProcessManager env → electron-store → settings UI → SSE notification. No broken import chains or missing references.</verify>
  <done>Integration chain verified end-to-end, no broken references, any issues documented</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 14: LlamaCpp LLM backend with settings UI, notifications, and model bundling</what-built>
  <how-to-verify>
    1. Start Electron app: `cd blackbox-desktop && npm start`
    2. Verify settings gear icon in AppBar
    3. Open settings — confirm "Local CPU (SmolLM3)" is default
    4. Check chat service status chip shows correct status
    5. If GGUF model is available: toggle chat service on, wait for model load, send a test analysis
    6. Verify toast notification appears when summary completes
    7. Switch to Ollama in settings — verify chat service restarts
    8. Switch back to Local CPU — verify it restarts again
    9. Close and reopen app — verify setting persists
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix. Note: if model file is not yet downloaded, steps 5-6 can be deferred to deployment testing.</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] All 5 plans executed successfully
- [ ] LlamaCppChatService implements ChatService interface
- [ ] Backend selection works for llamacpp, ollama, openai
- [ ] Electron settings persist LLM backend choice
- [ ] ProcessManager injects correct env vars per backend
- [ ] Frontend settings UI allows switching backends
- [ ] Toast notification fires on summary completion
- [ ] Build scripts updated for llama-cpp-python and model bundling
- [ ] No regressions to existing functionality
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Human verified end-to-end integration
- Phase 14 complete — full offline pipeline (transcribe -> diarize -> summarize) enabled
</success_criteria>

<output>
After completion, create `.planning/phases/14-guppy-llm-integration/14-05-SUMMARY.md`:

# Phase 14, Plan 05: End-to-End Verification Summary

**[Substantive one-liner]**

## Accomplishments
## Files Created/Modified
## Decisions Made
## Issues Encountered
## Next Step

Phase 14 complete. Full offline wargame analysis pipeline enabled with llamacpp backend.
</output>
