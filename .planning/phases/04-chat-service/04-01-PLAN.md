---
phase: 04-chat-service
plan: 01
type: execute
---

<objective>
Configure and run the ask-chat service with remote Ollama server connectivity.

Purpose: Enable LLM chat functionality by pointing the existing Ollama-based chat service to a remote Ollama server, avoiding local RAM consumption for model inference.
Output: Running ask-chat service on port 8083 connected to remote Ollama, verified with health check and test chat request.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase context:
@.planning/phases/01-infrastructure/01-02-SUMMARY.md
@.planning/phases/02-backend-setup/02-01-SUMMARY.md
@.planning/phases/03-diarization-rewrite/03-02-SUMMARY.md

# Key source files:
@ask-chat-service/src/services/ollama_chat_service.py
@ask-chat-service/src/services/__init__.py
@ask-chat-service/src/main.py
@ask-chat-service/.env.template

**Port assignments (avoid conflicts):**
- 8080: Conductor API
- 8081: Backend (Spring Boot)
- 8082: Diarization service
- 8083: Chat service (this phase)
- 5001: Conductor UI

**Tech available:**
- FastAPI with Conductor worker pattern (from diarization service)
- ollama Python SDK already in requirements.txt
- CHAT_SERVICE env var selects provider (ollama/openai)

**Constraining decisions:**
- Remote Ollama server for LLM inference (offloads RAM)
- OLLAMA_API_ENDPOINT and OLLAMA_MODEL_NAME configure the Ollama client

**User context:**
- User has remote Ollama server available (IP/hostname needed)
- User needs to know which Ollama model to use
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Configure remote Ollama server connection details</decision>
  <context>The chat service needs to connect to your remote Ollama server. I need the server URL and which model to use.</context>
  <options>
    <option id="provide-details">
      <name>Provide server details</name>
      <pros>Can configure and test immediately</pros>
      <cons>None</cons>
    </option>
    <option id="use-placeholder">
      <name>Use placeholder, configure later</name>
      <pros>Can proceed with setup, test when server available</pros>
      <cons>Cannot verify connectivity until details provided</cons>
    </option>
  </options>
  <resume-signal>Provide: OLLAMA_API_ENDPOINT (e.g., http://192.168.1.100:11434) and OLLAMA_MODEL_NAME (e.g., llama3.2, mistral, qwen2.5)</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Create .env file for chat service</name>
  <files>ask-chat-service/.env</files>
  <action>Create .env file with:
- CONDUCTOR_URL=http://localhost:8080 (local Docker Conductor)
- OLLAMA_API_ENDPOINT=[from checkpoint decision]
- OLLAMA_MODEL_NAME=[from checkpoint decision]
- CHAT_SERVICE=ollama (select Ollama provider)

Do NOT include OpenAI credentials (not needed for local setup).</action>
  <verify>cat ask-chat-service/.env shows all 4 required variables</verify>
  <done>.env file exists with CONDUCTOR_URL, OLLAMA_API_ENDPOINT, OLLAMA_MODEL_NAME, CHAT_SERVICE</done>
</task>

<task type="auto">
  <name>Task 2: Create venv and install dependencies</name>
  <files>ask-chat-service/</files>
  <action>Create Python virtual environment and install dependencies:
1. cd ask-chat-service
2. python3 -m venv venv
3. source venv/bin/activate
4. pip install -r requirements.txt

Note: Service uses FastAPI, conductor-python, ollama SDK - all lightweight dependencies.</action>
  <verify>ask-chat-service/venv/bin/python -c "import fastapi; import ollama; import conductor.client; print('OK')"</verify>
  <done>Virtual environment created with all dependencies installed</done>
</task>

<task type="auto">
  <name>Task 3: Start chat service and verify health</name>
  <files>ask-chat-service/src/main.py</files>
  <action>Start the chat service on port 8083:
1. cd ask-chat-service
2. source venv/bin/activate
3. source .env (export env vars)
4. python -m uvicorn src.main:app --host 0.0.0.0 --port 8083

Wait for "Application startup complete" message.

Note: main.py defaults to port 8080, override with --port 8083 to avoid Conductor conflict.</action>
  <verify>curl http://localhost:8083/health returns 200 OK</verify>
  <done>Service running on port 8083, health endpoint responding</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Chat service running on port 8083 with remote Ollama connectivity</what-built>
  <how-to-verify>
    1. Confirm service is running: curl http://localhost:8083/health
    2. Check Conductor UI at http://localhost:5001 for "ask_chat" task registration
    3. Verify no errors in service logs (check terminal where service started)
    4. Optional: Test Ollama connectivity by checking service doesn't throw connection errors on startup
  </how-to-verify>
  <resume-signal>Type "approved" if service is healthy, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] .env file exists with correct configuration
- [ ] Virtual environment created with dependencies
- [ ] Service running on port 8083
- [ ] Health endpoint returns 200
- [ ] No startup errors in logs
- [ ] "ask_chat" task visible in Conductor UI
</verification>

<success_criteria>

- Chat service running and healthy on port 8083
- Configured to use remote Ollama server
- Conductor task registered and visible
- No errors in service logs
</success_criteria>

<output>
After completion, create `.planning/phases/04-chat-service/04-01-SUMMARY.md`:

# Phase 4 Plan 01: Chat Service Summary

**[Substantive one-liner about what was accomplished]**

## Accomplishments

- [Key outcomes]

## Files Created/Modified

- `ask-chat-service/.env` - Local environment configuration

## Decisions Made

- Remote Ollama server: [URL]
- Model selected: [model name]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Phase Readiness

Ready for Phase 5: Frontend Setup
</output>
