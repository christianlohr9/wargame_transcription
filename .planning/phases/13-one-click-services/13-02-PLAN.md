---
phase: 13-one-click-services
plan: 02
type: execute
---

<objective>
Convert Python services from Conductor task workers to REST-only endpoints and wire Spring Boot to call them directly.

Purpose: Complete the Conductor removal by converting the Python side. Both Python services currently register as Conductor workers that poll for tasks. Instead, they should expose REST endpoints that the Spring Boot orchestration service calls directly via HTTP.
Output: Both Python services run as standalone FastAPI apps with REST endpoints. No Conductor dependency. Spring Boot PipelineOrchestrationService calls them via HTTP.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-one-click-services/13-RESEARCH.md
@.planning/phases/13-one-click-services/13-01-SUMMARY.md

# Key source files:
@speaker-diarization-service/src/lifecycle.py
@speaker-diarization-service/src/tasks/diarization_task.py
@speaker-diarization-service/src/main.py
@speaker-diarization-service/src/api/diarization_api.py
@speaker-diarization-service/src/services/whisperx_diarization_service.py
@speaker-diarization-service/requirements.txt
@ask-chat-service/src/lifecycle.py
@ask-chat-service/src/tasks/ask_chat_task.py
@ask-chat-service/src/main.py
@ask-chat-service/src/api/ask_chat_api.py
@ask-chat-service/requirements.txt

**Constraining decisions:**
- [Phase 11]: Lazy pyannote loading — transcription_only works without HF_TOKEN
- [Phase 12]: Python < 3.13 required for conductor-python (but we're removing conductor-python now)
- [Phase 12]: vad_filter=True on faster-whisper for full audio transcription
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert Python services from Conductor workers to REST-only</name>
  <files>
    speaker-diarization-service/src/lifecycle.py,
    speaker-diarization-service/src/tasks/diarization_task.py,
    speaker-diarization-service/src/main.py,
    speaker-diarization-service/src/api/diarization_api.py,
    speaker-diarization-service/requirements.txt,
    ask-chat-service/src/lifecycle.py,
    ask-chat-service/src/tasks/ask_chat_task.py,
    ask-chat-service/src/main.py,
    ask-chat-service/src/api/ask_chat_api.py,
    ask-chat-service/requirements.txt
  </files>
  <action>
    **Speaker Diarization Service:**

    1. Read diarization_task.py to understand the exact input/output contract:
       - Input: file_path (URL to audio file), language, pipeline_mode
       - Output: DiarizationModel fields (fragments, speakers, duration, etc.)

    2. Read the existing diarization_api.py — it likely already has a REST endpoint for transcription. Check if it already handles the same parameters the Conductor task uses. If so, the endpoint may only need minor adjustments.

    3. Ensure diarization_api.py has a POST endpoint that accepts: file_path (URL), language, pipeline_mode — and returns the full DiarizationModel as JSON. This is what the Spring Boot PipelineOrchestrationService will call.

    4. Remove lifecycle.py content related to Conductor (ConductorRunner, TaskHandler, task registration). Keep the FastAPI lifespan function but remove Conductor startup/shutdown. If lifecycle.py only does Conductor things, simplify to just model preloading or make it a no-op.

    5. Delete the tasks/ directory (diarization_task.py) — no longer needed.

    6. Remove `conductor-python` from requirements.txt.

    7. Update main.py to remove any Conductor lifecycle references.

    **Ask Chat Service:**

    1. Read ask_chat_task.py to understand the input/output contract:
       - Input: messages array, response_schema (from ChatHistoryDto)
       - Output: response string

    2. Read ask_chat_api.py — check existing REST endpoints. Ensure there's a POST endpoint that accepts the same inputs the Conductor task used and returns the LLM response.

    3. Remove lifecycle.py Conductor content. Delete tasks/ directory.

    4. Remove `conductor-python` from requirements.txt.

    5. Update main.py to remove Conductor lifecycle references.

    **For both services:** Keep the /health endpoint as-is. Keep all service logic (whisperx_diarization_service.py, chat services) untouched. Only remove the Conductor glue layer.

    **Avoid:** Don't change the actual diarization or chat logic. Don't change model loading. Don't change the service ports (8082, 8083). The goal is purely to remove the Conductor dependency while preserving all functionality.
  </action>
  <verify>
    `cd speaker-diarization-service && grep -r "conductor" src/ requirements.txt` returns no results.
    `cd ask-chat-service && grep -r "conductor" src/ requirements.txt` returns no results.
    `cd speaker-diarization-service && python -c "from src.main import app; print('OK')"` succeeds.
    `cd ask-chat-service && python -c "from src.main import app; print('OK')"` succeeds.
  </verify>
  <done>
    Both Python services have no Conductor dependency.
    REST endpoints exist for all operations previously handled by Conductor tasks.
    conductor-python removed from both requirements.txt.
    tasks/ directories removed.
    lifecycle.py simplified (no Conductor).
    Services still start and serve /health endpoint.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire Spring Boot orchestration to call Python REST endpoints</name>
  <files>
    blackbox/backend/blackbox_service/src/main/java/com/cgi/blackbox/service/implementation/PipelineOrchestrationService.java,
    blackbox/backend/blackbox_application/src/main/resources/application.yml,
    blackbox/.env
  </files>
  <action>
    1. Read the PipelineOrchestrationService created in Plan 01. It should already be making HTTP calls to the Python services. Verify the endpoint URLs match what the Python services actually expose.

    2. Verify the diarization call:
       - URL: `${BACKEND_DIARIZATION_BASE_URL}/transcriptions` (or whatever the actual endpoint path is — read diarization_api.py to confirm)
       - Method: POST
       - Body: { file_path, language, pipeline_mode }
       - Response: DiarizationModel JSON

    3. Verify the chat call:
       - URL: `${BACKEND_ASK_CHAT_BASE_URL}/chat` (or actual path from ask_chat_api.py)
       - Method: POST
       - Body: ChatHistoryDto JSON (messages array, response_schema)
       - Response: { response: string }

    4. If any endpoint paths don't match, fix either the orchestration service or the Python endpoints to align.

    5. Ensure timeout configuration is appropriate:
       - Diarization can take minutes for long audio files (especially with pyannote diarization)
       - Set HTTP client timeout to at least 600s (10 min) for diarization calls
       - Chat calls can also be slow with large prompts — 120s timeout

    6. Remove docker-compose services no longer needed. From blackbox/docker-compose.yml, remove:
       - conductor (the orchestration server)
       - redis (only needed by Conductor)
       - elasticsearch (only needed by Conductor)
       - rabbitmq (only needed by Conductor)
       Keep: mongodb, minio, tika (these are removed in Plan 03)

    **Avoid:** Don't change the Python service logic. Don't change ports. Don't modify the frontend — it communicates only with the Spring Boot backend.
  </action>
  <verify>
    `cd blackbox && mvn clean install -DskipTests` compiles without errors.
    `grep -c "conductor\|redis\|elasticsearch\|rabbitmq" blackbox/docker-compose.yml` shows significantly fewer references (ideally only comments or zero).
    Endpoint URLs in PipelineOrchestrationService match actual Python API paths.
  </verify>
  <done>
    PipelineOrchestrationService HTTP calls match actual Python REST endpoints.
    Timeouts configured appropriately for long-running ML tasks.
    Conductor, Redis, Elasticsearch, RabbitMQ removed from docker-compose.yml.
    Build succeeds.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] No Conductor references in either Python service
- [ ] Both Python services start without errors
- [ ] Spring Boot orchestration service endpoint URLs match Python API paths
- [ ] docker-compose.yml has 4 fewer services (Conductor, Redis, ES, RabbitMQ)
- [ ] `cd blackbox && mvn clean install -DskipTests` succeeds
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Both Python services are pure FastAPI REST apps (no Conductor)
- Spring Boot calls Python services directly via HTTP
- 4 infrastructure services removed from docker-compose
- No Conductor references anywhere in the codebase
</success_criteria>

<output>
After completion, create `.planning/phases/13-one-click-services/13-02-SUMMARY.md`
</output>
