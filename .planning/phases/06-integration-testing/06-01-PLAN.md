---
phase: 06-integration-testing
plan: 01
type: execute
---

<objective>
Verify complete end-to-end workflow: upload audio → diarization → analysis → chat

Purpose: Confirm all locally-running services work together — frontend communicates with backend, backend orchestrates Conductor workflows, AI services (diarization and chat) process requests successfully.
Output: Documented verification that the platform is functional end-to-end locally.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Prior phase summaries (dependency graph):**
@.planning/phases/01-infrastructure/01-02-SUMMARY.md
@.planning/phases/02-backend-setup/02-01-SUMMARY.md
@.planning/phases/03-diarization-rewrite/03-03-SUMMARY.md
@.planning/phases/04-chat-service/04-01-SUMMARY.md
@.planning/phases/05-frontend-setup/05-01-SUMMARY.md

**Key files:**
@blackbox/docker-compose.yml
@blackbox/.env
@blackbox/frontend/src/boot/axios.ts
@speaker-diarization-service/.env
@ask-chat-service/.env

**Service ports established:**
- Conductor API: 8080, UI: 5001
- Backend: 8081
- Diarization: 8082
- Chat: 8083
- Frontend: 9003
- MongoDB: 27017
- Elasticsearch: 9200
- Redis: 6379
- Tika: 9998
- MinIO: 9000/9001
- RabbitMQ: 5672/15672

**Workflow under test:**
- `builtin-transcription-workflow` triggered by POST `/workspaces/{id}/run`
- Tasks: transcribe → save_transcription → ask_chat (speakers) → save_determined_speakers → ask_chat (rounds) → execute_prompts

**Constraining decisions from prior phases:**
- Backend on port 8081 (avoid Conductor conflict)
- Diarization on port 8082
- Chat service on port 8083, using remote Ollama at ollama.island.a-p.team:11434
- Frontend on port 9003
- Conductor UI on port 5001 (macOS AirPlay conflict)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify all services are running</name>
  <files>None (verification only)</files>
  <action>
Check health of all required services before integration test:

1. Infrastructure services (Docker):
   - `docker compose ps` in blackbox/ directory
   - Expected: mongodb, rabbitmq, minio, conductor-server, elasticsearch, redis, tika all healthy

2. Backend service:
   - `curl http://localhost:8081/actuator/health` (Spring Boot health endpoint)
   - Expected: {"status":"UP"}

3. Diarization service:
   - `curl http://localhost:8082/health`
   - Expected: {"status":"ok"}

4. Chat service:
   - `curl http://localhost:8083/health`
   - Expected: {"status":"ok"}

5. Frontend dev server:
   - `curl -s -o /dev/null -w "%{http_code}" http://localhost:9003`
   - Expected: 200

If any service is not running, provide startup instructions.
  </action>
  <verify>All 5 service health checks pass</verify>
  <done>All services verified running: infrastructure (7), backend (1), diarization (1), chat (1), frontend (1)</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <action>Start any services that are not running</action>
  <instructions>
Based on the health check results, start any missing services:

**If infrastructure services not running:**
```bash
cd blackbox && docker compose up -d
```

**If backend not running:**
```bash
cd blackbox && source .env && java -jar backend/blackbox_application/target/blackbox_application-0.0.1-SNAPSHOT.jar
```

**If diarization service not running:**
```bash
cd speaker-diarization-service && source .env && python -m uvicorn src.main:app --host 0.0.0.0 --port 8082
```

**If chat service not running:**
```bash
cd ask-chat-service && source .env && python -m uvicorn src.main:app --host 0.0.0.0 --port 8083
```

**If frontend not running:**
```bash
cd blackbox/frontend && npm run dev -- --port 9003
```

Note: Each service needs its own terminal. Wait for all services to be healthy before continuing.
  </instructions>
  <verification>Re-run health checks from Task 1 to confirm all services are up</verification>
  <resume-signal>Type "services ready" when all services are running, or "skip" if all were already running</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Integration test workflow — uploading audio and verifying end-to-end processing</what-built>
  <how-to-verify>
1. Open browser to http://localhost:9003
2. Create a new workspace:
   - Click "+" or workspace creation button
   - Enter a name (e.g., "Integration Test")
   - Select or create a wargame setup if prompted
3. Upload a test audio file:
   - Click the "+" button in the workspace
   - Select an audio file (MP3, WAV, etc.) — any short audio with speech works
   - Wait for upload to complete
4. Trigger processing:
   - Close the audio file dialog (click "Bestätigen")
   - This triggers the Conductor workflow
5. Monitor progress:
   - Check Conductor UI at http://localhost:5001
   - Look for running workflow "builtin-transcription-workflow"
   - Watch for task completion: transcribe → save_transcription → ask_chat → ...
6. Verify results in frontend:
   - Transcript should appear with speaker labels (Speaker A, Speaker B, etc.)
   - After chat analysis, speaker names may be resolved
   - Loading spinner should disappear when transcription is complete

**Expected timeline:**
- Diarization: ~0.68x realtime on CPU (30s audio = ~20s processing)
- Chat analysis: Variable depending on Ollama response time
- Total: May take several minutes for complete workflow

**What success looks like:**
- Transcript timeline visible with speaker segments
- Each segment shows transcribed text with timestamps
- No error messages in browser console
  </how-to-verify>
  <resume-signal>Type "integration passed" if workflow completed successfully, or describe any errors encountered</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] All 5 service categories verified running
- [ ] Frontend loads without errors
- [ ] Workspace creation works
- [ ] Audio upload completes
- [ ] Conductor workflow executes (visible in Conductor UI)
- [ ] Diarization task completes successfully
- [ ] Transcript appears in frontend
- [ ] No critical errors in any service logs
</verification>

<success_criteria>
- All services running and healthy
- Complete workflow execution: upload → diarization → transcription display
- Conductor workflow visible and completes without errors
- User can see transcribed audio with speaker labels in frontend
- Platform is usable end-to-end without external API dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/06-integration-testing/06-01-SUMMARY.md`:

# Phase 6 Plan 1: Integration Testing Summary

**[Substantive description of integration test results]**

## Accomplishments

- [Services verified]
- [Workflow execution results]
- [Any issues resolved]

## Test Results

| Step | Status | Notes |
|------|--------|-------|
| Infrastructure health | ✓/✗ | ... |
| Backend health | ✓/✗ | ... |
| Diarization health | ✓/✗ | ... |
| Chat service health | ✓/✗ | ... |
| Frontend accessible | ✓/✗ | ... |
| Workspace creation | ✓/✗ | ... |
| Audio upload | ✓/✗ | ... |
| Conductor workflow | ✓/✗ | ... |
| Transcript display | ✓/✗ | ... |

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Milestone 1 complete — all local development setup verified working end-to-end.
</output>
