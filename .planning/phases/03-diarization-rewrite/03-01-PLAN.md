---
phase: 03-diarization-rewrite
plan: 01
type: execute
---

<objective>
Implement WhisperX + pyannote speaker diarization service to replace AssemblyAI.

Purpose: Enable fully local speaker diarization without external API dependencies.
Output: Working WhisperXDiarizationService class integrated into the service.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-diarization-rewrite/03-RESEARCH.md

# Prior phase context:
@.planning/phases/01-infrastructure/01-02-SUMMARY.md

# Key source files:
@speaker-diarization-service/src/services/diarization_service.py
@speaker-diarization-service/src/services/assemblyai_diarization_service.py
@speaker-diarization-service/src/services/__init__.py
@speaker-diarization-service/src/models/diarization_model.py
@speaker-diarization-service/requirements.txt
@speaker-diarization-service/.env.template

**Tech stack available:**
- FastAPI service structure with Conductor integration
- Pydantic models for DiarizationModel
- Abstract DiarizationService base class

**Established patterns:**
- Service instantiation in services/__init__.py
- ABC pattern for diarization service implementations
- Environment variables for configuration

**Constraining decisions:**
- CPU-only inference (no GPU on target hardware)
- Batch processing acceptable (overnight jobs)
- Use WhisperX integrated pipeline (don't hand-roll)
- Use "small" model with int8 compute type
- Load models once at startup

**From research (03-RESEARCH.md):**
- WhisperX integrates faster-whisper + wav2vec2 + pyannote
- CPU: device="cpu", compute_type="int8", batch_size=4
- Models must be cached at startup (30-60s load time)
- Requires HF_TOKEN for pyannote models
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update requirements.txt for WhisperX</name>
  <files>speaker-diarization-service/requirements.txt</files>
  <action>
Replace assemblyai dependency with WhisperX and CPU-only torch:

1. Remove `assemblyai==0.46.0`
2. Add CPU-only torch (must be installed first for CPU-only):
   `--extra-index-url https://download.pytorch.org/whl/cpu`
   `torch`
3. Add `whisperx` (this pulls in faster-whisper, pyannote.audio transitively)

Keep existing fastapi and conductor-python dependencies.

Note: The --extra-index-url line tells pip to use CPU-only PyTorch wheels.
  </action>
  <verify>Review requirements.txt shows whisperx and torch, no assemblyai</verify>
  <done>requirements.txt updated with WhisperX dependencies, assemblyai removed</done>
</task>

<task type="auto">
  <name>Task 2: Create WhisperXDiarizationService implementation</name>
  <files>speaker-diarization-service/src/services/whisperx_diarization_service.py</files>
  <action>
Create new file implementing DiarizationService ABC using WhisperX pipeline.

Key implementation details (from 03-RESEARCH.md):
1. **__init__**: Load models ONCE at startup:
   - whisperx.load_model("small", "cpu", compute_type="int8")
   - whisperx.DiarizationPipeline(use_auth_token=HF_TOKEN, device="cpu")
   - Get HF_TOKEN from environment variable (raise if missing)

2. **diarize(audio_file, language)**: Execute full pipeline:
   - whisperx.load_audio(audio_file)
   - model.transcribe(audio, batch_size=4, language=language)
   - whisperx.load_align_model() + whisperx.align() for word timestamps
   - diarize_pipeline(audio) for speaker segments
   - whisperx.assign_word_speakers() to merge

3. **Output mapping**: Convert WhisperX result to DiarizationModel:
   - audio_duration: int(len(audio) / 16000 * 1000) -- audio is 16kHz, convert to ms
   - available_speakers: Extract unique speakers, format as "Speaker 00", "Speaker 01"
   - fragments: Map segments to DiarizationFragmentModel with times in milliseconds

Follow the service implementation pattern from 03-RESEARCH.md code_examples section.
Avoid: Loading models per-request, float16 on CPU, batch_size > 4.
  </action>
  <verify>File exists with WhisperXDiarizationService class inheriting DiarizationService</verify>
  <done>whisperx_diarization_service.py created with full implementation</done>
</task>

<task type="auto">
  <name>Task 3: Wire up WhisperXDiarizationService and update env template</name>
  <files>speaker-diarization-service/src/services/__init__.py, speaker-diarization-service/.env.template</files>
  <action>
1. **services/__init__.py**:
   - Add import: `from .whisperx_diarization_service import WhisperXDiarizationService`
   - Change instantiation: `diarization_service: DiarizationService = WhisperXDiarizationService()`
   - Keep AssemblyAI import for reference (don't delete the class)

2. **.env.template**:
   - Remove: `ASSEMBLYAI_API_KEY=your_assemblyai_api_key_here`
   - Add: `HF_TOKEN=your_huggingface_token_here`
   - Keep CONDUCTOR_URL and other existing vars
   - Add comment explaining HF_TOKEN is for pyannote model access

Note: The service port is 8080 in main.py. Backend on 8081 calls this.
  </action>
  <verify>grep -n "WhisperX" speaker-diarization-service/src/services/__init__.py shows import and instantiation</verify>
  <done>Service wired to use WhisperX, env template updated for HF_TOKEN</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] requirements.txt has whisperx, torch (CPU), no assemblyai
- [ ] whisperx_diarization_service.py exists and follows DiarizationService ABC
- [ ] services/__init__.py instantiates WhisperXDiarizationService
- [ ] .env.template has HF_TOKEN instead of ASSEMBLYAI_API_KEY
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Code follows patterns from 03-RESEARCH.md
- No syntax errors in Python files
</success_criteria>

<output>
After completion, create `.planning/phases/03-diarization-rewrite/03-01-SUMMARY.md`
</output>
